<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>{{pageTitle}}</title>
</head>
<body>
    <h1>{{pageTitle}}</h1>
    <ul>
        {{#each destinations}}
            <li>{{this.name}} - {{this.price}}</li>
        {{/each}}
    </ul>
</body>
</html>
